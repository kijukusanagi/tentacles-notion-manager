<!--
v1.4 fresh-setup test script. Run against branch v1.4-hardening (6 commits on main@5fe6c97).
Fill the Record column during the run. Results gate the v1.4 tag.
-->

# v1.4 Fresh-Setup Test

**Setup (not timed):** fresh Notion workspace, template duplicated into a teamspace, new Claude Project, Notion connector authorized to that workspace, nothing in Files. Same tester and same first-ticket content as the v1.3 baseline.

**Timing:** T0 = first message sent. **T4 = config uploaded and new conversation started** — the point comparable to the v1.3 baseline's end. T5 = deferred doc-page cost, recorded separately.

**Blocking unknown:** `agent/system-prompt.md` is 81,263 bytes / 79,560 characters (`wc -m`) (v1.3: 66,327). Project Instructions limits are counted in characters, so 79,560 is the figure that matters for this checkpoint. (Count changed at 2d6c18f.) Checkpoint 0 resolves whether Project Instructions accept it. **If it rejects, stop and report before continuing.**

---

## Happy path

| # | Step | Expected | Record |
|---|---|---|---|
| 0 | Paste `agent/system-prompt.md` into Instructions | Accepted, or rejected for length | **PASS** — 79,560 characters accepted; re-pasted at 80,490 characters after the final additions — PASS |
| 1 | New conversation, type `hello`. **Start T0.** | Onboarding greeting. Says "15–30 minutes" (v1.3 said "about 5"). No config detected, no version talk. | **NOT RUN** — timed setup run deferred |
| 2 | Let Step 1 run | Searches "OS Layer"; asks which teamspace only if >1; reports "Found all 8 core databases in [teamspace]" | **NOT RUN** — timed setup run deferred |
| 3 | Step 2 personalization: company, prefix, team, codes (accept defaults), no clients, default effort/capacity | **Before** Apply Configuration, agent fetches Tickets and Tasks schema — look for `notion-fetch` before any `update-data-source`. Project Code SET SELECT includes all pre-existing options. Confirmation says Type/Hours were "already present" **or** "were added". | **NOT RUN** — timed setup run deferred |
| 4 | Watch for doc-page rewrite | **None.** No "Documentation pages are updated" message; no `update-page` calls on Agent Config / Master List / Interface Spec | **NOT RUN** — timed setup run deferred |
| 5 | Step 2.5: answer "no" to migration | Proceeds to Learn by Doing | **NOT RUN** — timed setup run deferred |
| 6 | Give the baseline's first ticket | Scope check → suggest → confirm → create. Source=Agent, Requester=tentacles-setup. Then task: Status To Do, Hours Estimated auto-filled. | **NOT RUN** — timed setup run deferred |
| 7 | Step 4: config generation | First key `"tentacles_config": true`, `"version": "1.4"`, `"extensions": {"databases": []}`, **no** `_example_entry`, **no** `_comment`, **no** `{PLACEHOLDER}`. Filename suggested as `<prefix>-tentacles-config.json`. Closing text includes "Say 'update docs' any time". | **NOT RUN** — timed setup run deferred |
| 8 | Save with suggested name, upload to Files, new conversation, `hello` | Operations Mode, silently — no onboarding, no version mention | **NOT RUN** — timed setup run deferred |
| 9 | `doctor` | Read-only drift table (or one-line "no drift"), plus separate "Not in config (info)" list. No Notion writes in tool log. Offers regen only if drift. | **NOT RUN** — timed setup run deferred |
| 9b | Register a throwaway extension DB in the config, run `doctor` (clean), **delete the DB in Notion**, run `doctor` again | Second run reports the extension as `UNREACHABLE` **and** the Tickets relation to it as `UNREACHABLE`; a view on it (if you made one) as `UNREACHABLE (view)`; regen is offered for neither. | ☐ pass ☐ fail (added in v1.4.2 — reproduces the failure that motivated the relation-target walk) |
| 10 | `health check` | Alerts summary (🔴/🟡/🔵), **not** the doctor table | **NOT RUN** — timed setup run deferred |
| 11 | `add a project code <PREFIX>-TRAIN for training` | Fetches Tickets schema (or reuses doctor's), SET SELECT with every existing option + new one; reminds you to run doctor. Verify in Notion nothing vanished. | **NOT RUN** — timed setup run deferred |
| 12 | Create two tickets in the same conversation | **One** schema fetch for Tickets total — second ticket reuses cache, no second `notion-fetch` | **NOT RUN** — timed setup run deferred |
| 13 | `update docs` | Runs the four doc-page updates; confirms "Documentation pages are updated…" | **NOT RUN** — timed setup run deferred |

## Negative path — this is what actually tests B1

| # | Step | Expected | Record |
|---|---|---|---|
| 14 | Also upload `agent/config-template.json` to Files; new conversation; `hello`. Remove after. | Still Operations Mode; template ignored (marker false) | **PASS** — template with marker false silently ignored, Operations Mode entered |
| 15 | Upload a second copy of the real config under another name; new conversation. Remove after. | "Which config is current?" — stops **before** any Notion call | **PASS** — two marked configs, stopped and asked, no Notion call |
| 16 | Upload a copy of the template with `"tentacles_config": true` hand-edited in, placeholders intact; new conversation. Remove after. | Refusal: "still contains placeholder values ({list})…" | **PASS** — hand-flipped template refused, placeholders named |
| 17 | Separate project: a **v1.3** config in Files + v1.4 prompt; `hello` | Bootstrap path → Operations Mode → "Your config was created with v1.3… v1.4… run the migration?" → yes → outputs config with marker + empty extensions, **zero Notion calls**. Upload, new conversation → silent ops. | **NOT RUN** — needs a genuine v1.3-shaped config; bootstrap path untested |

Note on 17: this needs a genuine v1.3-shaped config. The CL config in project knowledge declares `cl-1.0`, so it won't exercise the path. Either hand-build a v1.3 stub or mark 17 **untested** and say so.

---

## Results

- **T4 vs v1.3 baseline:** not measured — happy path (1–13) not run; timed setup run deferred
- **T5 (deferred doc-page cost):** not measured
- **Checkpoint 0:** PASS — 79,560 characters accepted in Project Instructions; re-paste at 80,490 characters (e339cf0) also accepted. The blocking unknown is resolved.
- **Negative path 14–17:** 14 PASS, 15 PASS, 16 PASS; 17 NOT RUN (no v1.3-shaped config available)
- **Friction log:** pending the timed run
- **Found during 14–16:** the placeholder check passes fake-but-not-placeholder IDs (`ds_fake_tickets`); the agent itself flagged the gap → UUID-shape warn-once check added as Startup Config Validation check 4 (prompt-only). Refusal messages reordered to lead with the action.
