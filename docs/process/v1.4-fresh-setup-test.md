<!--
v1.4 fresh-setup test script. Run against branch v1.4-hardening (6 commits on main@5fe6c97).
Fill the Record column during the run. Results gate the v1.4 tag.
-->

# v1.4 Fresh-Setup Test

**Setup (not timed):** fresh Notion workspace, template duplicated into a teamspace, new Claude Project, Notion connector authorized to that workspace, nothing in Files. Same tester and same first-ticket content as the v1.3 baseline.

**Timing:** T0 = first message sent. **T4 = config uploaded and new conversation started** — the point comparable to the v1.3 baseline's end. T5 = deferred doc-page cost, recorded separately.

**Blocking unknown:** `agent/system-prompt.md` is 81,259 chars (v1.3: 66,327). Checkpoint 0 resolves whether Project Instructions accept it. **If it rejects, stop and report before continuing.**

---

## Happy path

| # | Step | Expected | Record |
|---|---|---|---|
| 0 | Paste `agent/system-prompt.md` into Instructions | Accepted, or rejected for length | Accepted 81,259 chars: ☐ Y ☐ N |
| 1 | New conversation, type `hello`. **Start T0.** | Onboarding greeting. Says "15–30 minutes" (v1.3 said "about 5"). No config detected, no version talk. | T0 = |
| 2 | Let Step 1 run | Searches "OS Layer"; asks which teamspace only if >1; reports "Found all 8 core databases in [teamspace]" | T1 = <br>Teamspace ambiguity: |
| 3 | Step 2 personalization: company, prefix, team, codes (accept defaults), no clients, default effort/capacity | **Before** Apply Configuration, agent fetches Tickets and Tasks schema — look for `notion-fetch` before any `update-data-source`. Project Code SET SELECT includes all pre-existing options. Confirmation says Type/Hours were "already present" **or** "were added". | T2 = <br>Type/Hours message: <br>Any Project Code option dropped (check Notion): |
| 4 | Watch for doc-page rewrite | **None.** No "Documentation pages are updated" message; no `update-page` calls on Agent Config / Master List / Interface Spec | ☐ pass ☐ fail |
| 5 | Step 2.5: answer "no" to migration | Proceeds to Learn by Doing | — |
| 6 | Give the baseline's first ticket | Scope check → suggest → confirm → create. Source=Agent, Requester=tentacles-setup. Then task: Status To Do, Hours Estimated auto-filled. | T3 = <br>Serialized ID: |
| 7 | Step 4: config generation | First key `"tentacles_config": true`, `"version": "1.4"`, `"extensions": {"databases": []}`, **no** `_example_entry`, **no** `_comment`, **no** `{PLACEHOLDER}`. Filename suggested as `<prefix>-tentacles-config.json`. Closing text includes "Say 'update docs' any time". | Grep `{` + capitals: <br>Top-level key count: |
| 8 | Save with suggested name, upload to Files, new conversation, `hello` | Operations Mode, silently — no onboarding, no version mention | **T4 =** |
| 9 | `doctor` | Read-only drift table (or one-line "no drift"), plus separate "Not in config (info)" list. No Notion writes in tool log. Offers regen only if drift. | Drift rows: <br>INFO rows: |
| 10 | `health check` | Alerts summary (🔴/🟡/🔵), **not** the doctor table | ☐ pass ☐ fail |
| 11 | `add a project code <PREFIX>-TRAIN for training` | Fetches Tickets schema (or reuses doctor's), SET SELECT with every existing option + new one; reminds you to run doctor. Verify in Notion nothing vanished. | Options before: <br>Options after: |
| 12 | Create two tickets in the same conversation | **One** schema fetch for Tickets total — second ticket reuses cache, no second `notion-fetch` | Fetch count: |
| 13 | `update docs` | Runs the four doc-page updates; confirms "Documentation pages are updated…" | **T5 =** (report separately) |

## Negative path — this is what actually tests B1

| # | Step | Expected | Record |
|---|---|---|---|
| 14 | Also upload `agent/config-template.json` to Files; new conversation; `hello`. Remove after. | Still Operations Mode; template ignored (marker false) | ☐ pass ☐ fail |
| 15 | Upload a second copy of the real config under another name; new conversation. Remove after. | "Which config is current?" — stops **before** any Notion call | ☐ pass ☐ fail |
| 16 | Upload a copy of the template with `"tentacles_config": true` hand-edited in, placeholders intact; new conversation. Remove after. | Refusal: "still contains placeholder values ({list})…" | ☐ pass ☐ fail |
| 17 | Separate project: a **v1.3** config in Files + v1.4 prompt; `hello` | Bootstrap path → Operations Mode → "Your config was created with v1.3… v1.4… run the migration?" → yes → outputs config with marker + empty extensions, **zero Notion calls**. Upload, new conversation → silent ops. | ☐ pass ☐ fail <br>Any Notion call = **fail** |

Note on 17: this needs a genuine v1.3-shaped config. The CL config in project knowledge declares `cl-1.0`, so it won't exercise the path. Either hand-build a v1.3 stub or mark 17 **untested** and say so.

---

## Results

- **T4 vs v1.3 baseline:** 
- **T5 (deferred doc-page cost):** 
- **Checkpoint 0:** 
- **Negative path 14–17:** 
- **Friction log:**
