# Tentacles v1.2 — Tier 1: Agent → Operator

## Overview

v1.2 transforms Tentacles from a reactive tracker into a proactive operational partner. Three interconnected features ship together because they form a dependency chain:

1. **Effort Logging** (foundation) — adds time tracking to Tasks
2. **Proactive Alerting** (biggest visible impact) — agent surfaces problems without being asked
3. **Capacity Planning** (builds on 1 + 2) — translates effort into workload visibility

---

## Migration Registry Entry

```
## v1.1 → v1.2
Summary: Added Effort Logging, Proactive Alerting, and Capacity Planning.
Schema changes:
  - Tasks: ADD COLUMN "Hours Spent" NUMBER
  - Tasks: ADD COLUMN "Hours Estimated" NUMBER
  - Tickets: ADD COLUMN "Type" SELECT("Request", "Bug", "Decision", "Alert", "Proposal")
Config changes:
  - Add top-level `effort` section with hours_mapping and settings
  - Add top-level `alerts` section with checks and thresholds
  - Add top-level `capacity` section with per-user settings
  - databases.tasks.optional_fields: add "Hours Spent", "Hours Estimated"
  - databases.tickets.enums: add "Type" entry
  - Update `version` to "1.2"
  - Update `system_prompt_version` to "1.2"
  - Update `changelog`
Steps:
  1. Use MCP update-data-source on Tasks to add "Hours Spent" NUMBER and "Hours Estimated" NUMBER
  2. Use MCP update-data-source on Tickets to add "Type" SELECT("Request", "Bug", "Decision", "Alert", "Proposal")
  3. Add effort, alerts, and capacity sections to config
  4. Update config version fields
  5. Regenerate config file for user to re-upload
```

---

# FEATURE 1: EFFORT LOGGING

## Schema Changes

### Tasks Database

Add two new number properties:

```sql
ALTER COLUMN ADD "Hours Spent" NUMBER;
ALTER COLUMN ADD "Hours Estimated" NUMBER;
```

**Hours Spent** — Actual hours logged by user or agent. Populated when:
- User says "log X hours on [task]"
- Agent prompts after marking a task Done (if `effort.prompt_on_completion` is true)
- User provides time during task update

**Hours Estimated** — Auto-populated from the Effort Estimate field using the config mapping. When a task's Effort Estimate is set, the agent writes the corresponding hours value. Can be manually overridden.

### No Changes to Other Databases

Ticket-level and engagement-level time aggregation is done via agent queries (rollup across linked tasks), not via Notion formulas. This avoids schema bloat and keeps the calculation logic in the agent where it can be smarter.

## Config Additions

```json
{
  "effort": {
    "hours_mapping": {
      "XS": 1,
      "S": 2,
      "M": 4,
      "L": 8,
      "XL": 16
    },
    "prompt_on_completion": true,
    "track_variance": true,
    "default_log_unit": "hours"
  }
}
```

| Field | Type | Default | Purpose |
|-------|------|---------|---------|
| `hours_mapping` | object | XS:1...XL:16 | Maps Effort Estimate values to hour equivalents |
| `prompt_on_completion` | boolean | true | Ask "how long did this take?" when marking tasks Done |
| `track_variance` | boolean | true | Calculate and report estimated vs actual differences |
| `default_log_unit` | string | "hours" | Display unit for time values |

## System Prompt Additions

### Add to Operations Mode — Per-Database Operations — Tasks section:

```
### Effort Tracking

When the user or agent sets an **Effort Estimate** on a task, auto-populate
**Hours Estimated** using the config's `effort.hours_mapping`:
  XS → 1h, S → 2h, M → 4h, L → 8h, XL → 16h

When marking a task **Done**:
  1. If `effort.prompt_on_completion` is true and Hours Spent is empty:
     Ask "How many hours did this take?" before completing the task.
  2. If the user provides hours, set Hours Spent.
  3. If the user says "skip" or "not sure", leave Hours Spent empty and proceed.
  4. If `effort.track_variance` is true and both Hours Estimated and Hours Spent
     are populated, note the variance in the summary comment:
     "Completed in {actual}h (estimated {estimated}h, {over/under} by {diff}h)"

When the user says "log [N] hours on [task]":
  1. Find the task by name or ID.
  2. Set Hours Spent to N (or add N to existing value if hours are already logged).
  3. Confirm: "Logged {N}h on '{task name}'. Total: {total}h."

### Time Rollup Queries

When asked about time spent on a ticket, project, engagement, or any higher-level entity:
  1. Find all tasks linked to the entity (via Source Ticket → Ticket → related DBs).
  2. Sum Hours Spent across all linked tasks.
  3. Sum Hours Estimated across all linked tasks.
  4. Present: "Total: {spent}h spent / {estimated}h estimated across {count} tasks."
  5. If `effort.track_variance` is true, include: "Overall variance: {over/under} by {diff}h ({percentage}%)"

When asked "how much time have we spent on [X]?" or "time report for [X]":
  1. Identify the entity (client, project, engagement, ticket).
  2. Trace all linked tasks.
  3. Group by: assignee, sprint, project code — whatever provides the most useful breakdown.
  4. Present a summary table.
```

## Agent Patterns

```markdown
### Log Time on a Task

> "Log 3 hours on the API integration task"

The agent finds the task, adds hours to the Hours Spent field, and confirms. If hours were already logged, it adds to the existing total.

### Time Report for a Project

> "How much time have we spent on the website redesign?"

The agent traces all tasks linked to the project (via tickets and direct relations), sums Hours Spent, and presents a breakdown by person and by task. Includes estimated vs actual variance if both values exist.

### Sprint Time Summary

> "How many hours did we log this sprint?"

The agent queries all tasks in the current sprint with Hours Spent > 0, groups by assignee, and shows per-person and total hours against the sprint capacity (if capacity planning is configured).
```

---

# FEATURE 2: PROACTIVE ALERTING & ANOMALY DETECTION

## Concept

The agent runs a configurable set of "health checks" that scan across all 8 databases for problems, risks, and opportunities. Checks run automatically during morning briefings or on demand. Critical findings can auto-generate tickets.

## No Schema Changes Required

Alerting is purely an agent behavior — it reads existing data and applies rules. The only schema addition is the `Type` select on Tickets (added in the migration) so alert-generated tickets can be distinguished from normal work.

## Config Additions

```json
{
  "alerts": {
    "enabled": true,
    "run_on_briefing": true,
    "auto_ticket_on_critical": true,
    "checks": {
      "stale_tickets": {
        "enabled": true,
        "threshold_days": 7,
        "severity": "warning",
        "description": "Tickets not Done/Closed with no update in N days"
      },
      "stale_critical_tickets": {
        "enabled": true,
        "threshold_hours": 24,
        "severity": "critical",
        "applies_to": ["P0"],
        "description": "P0 tickets with no update in N hours"
      },
      "stale_high_tickets": {
        "enabled": true,
        "threshold_days": 3,
        "severity": "warning",
        "applies_to": ["P1"],
        "description": "P1 tickets with no update in N days"
      },
      "overloaded_assignees": {
        "enabled": true,
        "max_open_tasks": 8,
        "severity": "warning",
        "description": "Person with more than N open tasks"
      },
      "orphan_tasks": {
        "enabled": true,
        "severity": "info",
        "description": "Tasks with no Source Ticket link"
      },
      "unassigned_work": {
        "enabled": true,
        "severity": "warning",
        "min_status": "Triaged",
        "description": "Tickets/tasks past New/Backlog with no assignee"
      },
      "empty_engagements": {
        "enabled": true,
        "threshold_days": 30,
        "severity": "warning",
        "description": "Active engagements with $0 billed and older than N days"
      },
      "upcoming_deadlines": {
        "enabled": true,
        "lookahead_days": 3,
        "severity": "info",
        "description": "Tickets/tasks with due dates in the next N days"
      },
      "sprint_overflow": {
        "enabled": true,
        "severity": "warning",
        "description": "Current sprint has more estimated hours than team capacity",
        "requires": "capacity"
      },
      "effort_variance": {
        "enabled": true,
        "threshold_percentage": 50,
        "severity": "info",
        "description": "Tasks where actual hours exceed estimate by N%",
        "requires": "effort"
      }
    }
  }
}
```

## System Prompt Additions

### Add new section after Operations Mode — Standard Workflows:

```
# ═══════════════════════════════════════════
# PROACTIVE ALERTING
# ═══════════════════════════════════════════

The agent runs health checks across all databases to surface problems,
risks, and opportunities. This replaces the need for manual status-chasing.

## When Alerts Run

1. **Morning briefing** — If `alerts.run_on_briefing` is true, every "morning
   briefing" or "morning check-in" request starts with an alert summary before
   the status report. Alerts appear at the TOP of the briefing.

2. **On demand** — "Show me alerts", "any problems?", "health check",
   "what needs attention?"

3. **Background awareness** — During any operation, if the agent notices
   something that matches an alert condition (e.g., assigning a task to someone
   already overloaded), mention it inline: "Heads up — Jordan now has 9 open
   tasks, which is above the 8-task threshold."

## Alert Severity Levels

🔴 **Critical** — Requires immediate action. Examples: P0 tickets stale >24h,
   engagement billing anomalies. If `alerts.auto_ticket_on_critical` is true,
   the agent creates a ticket: Title = "[ALERT] {description}", Project Code =
   {PREFIX}-OPS, Priority = P0, Source = Agent, Type = Alert.

🟡 **Warning** — Needs attention soon. Examples: P1 tickets stale >3 days,
   assignee overload, unassigned triaged work, empty engagements.

🔵 **Info** — Worth knowing but not urgent. Examples: orphan tasks, upcoming
   deadlines, effort variance, sprint capacity notes.

## Running Health Checks

For each enabled check in `alerts.checks`, run the corresponding query.
Present results grouped by severity (critical first, then warning, then info).

### Check: stale_tickets
Query: Tickets where Status NOT IN (Done, Closed) AND last_edited_time < (now - threshold_days)
Present: List with ticket ID, title, days since last update, assignee

### Check: stale_critical_tickets
Query: Tickets where Priority IN (applies_to) AND Status NOT IN (Done, Closed) AND last_edited_time < (now - threshold_hours)
Present: List with ticket ID, title, hours since last update, assignee — flag as CRITICAL

### Check: stale_high_tickets
Query: Tickets where Priority IN (applies_to) AND Status NOT IN (Done, Closed) AND last_edited_time < (now - threshold_days)
Present: List with ticket ID, title, days since last update, assignee

### Check: overloaded_assignees
Query: For each user, count tasks where Status IN (To Do, In Progress, In Review)
Present: Users exceeding max_open_tasks with their task count and list of tasks

### Check: orphan_tasks
Query: Tasks where Source Ticket relation is empty AND Status NOT IN (Done)
Present: List of tasks with no parent ticket

### Check: unassigned_work
Query: Tickets where Assignee is empty AND Status NOT IN (New, Closed)
       Tasks where Assignee is empty AND Status NOT IN (Backlog, Done)
Present: List of unassigned items that probably should have an owner

### Check: empty_engagements
Query: Engagements where Status = Active AND Billed to Date = 0 AND Created > threshold_days ago
Present: Engagements that may need billing attention

### Check: upcoming_deadlines
Query: Tickets and Tasks where Due Date is within lookahead_days from today AND Status NOT IN (Done, Closed)
Present: Items due soon, sorted by date

### Check: sprint_overflow (requires capacity config)
Query: Sum Hours Estimated for all tasks in current sprint, compare to total team capacity
Present: Sprint load vs capacity with percentage

### Check: effort_variance (requires effort config)
Query: Tasks where Hours Spent > 0 AND Hours Estimated > 0 AND (Hours Spent / Hours Estimated) > (1 + threshold_percentage/100)
Present: Tasks running over estimate with the variance percentage

## Alert Output Format

"## 🚨 Alerts

🔴 **Critical** (1)
  - P0 ticket 'Production outage' has had no update in 26 hours (assigned: Jordan)

🟡 **Warning** (3)
  - 2 tickets stale >7 days: 'API redesign' (12 days), 'Onboarding flow' (9 days)
  - Jordan has 9 open tasks (threshold: 8)
  - Engagement 'Acme Corp Q1' is Active with $0 billed after 45 days

🔵 **Info** (2)
  - 3 tasks due in the next 3 days
  - Task 'Design mockups' ran 6h over estimate (10h actual vs 4h estimated)

---
[Proceed to morning briefing / status report]"

## Alert-Generated Tickets

When creating an alert ticket:
  - Title: "[ALERT] {short description}"
  - Project Code: {PREFIX}-OPS
  - Priority: P0 for critical alerts, P1 for warning alerts that auto-ticket
  - Source: Agent
  - Type: Alert
  - Description: Full alert details including which check triggered it,
    the threshold that was exceeded, and affected items with links
  - Do NOT create duplicate alert tickets. Before creating, search for existing
    open tickets with "[ALERT]" in the title matching the same issue. If found,
    add a comment to the existing ticket instead.
```

## Agent Patterns

```markdown
### Morning Briefing with Alerts

> "Morning briefing"

The agent runs all enabled health checks first, presents the alert summary, then delivers the standard briefing: in-progress work, blocked items, due dates, and cross-database status.

### Dedicated Health Check

> "Run a health check" / "Any problems I should know about?"

Full alert scan across all databases. The agent presents findings grouped by severity and offers to take action on any critical or warning items.

### Targeted Alert Check

> "Are any engagements falling behind on billing?"

The agent runs the specific relevant check (empty_engagements) and presents results. Can combine with other checks if the question implies broader scope.
```

---

# FEATURE 3: CAPACITY PLANNING

## No Additional Schema Changes

Capacity planning uses the Hours Estimated and Hours Spent fields added by Effort Logging, plus existing Effort Estimate, Assignee, Sprint, and Status fields on Tasks.

## Config Additions

```json
{
  "capacity": {
    "enabled": true,
    "sprint_duration_weeks": 2,
    "default_hours_per_sprint": 30,
    "warning_threshold_percent": 80,
    "users": {},
    "assignment_guard": true
  }
}
```

| Field | Type | Default | Purpose |
|-------|------|---------|---------|
| `sprint_duration_weeks` | number | 2 | Length of a sprint in weeks |
| `default_hours_per_sprint` | number | 30 | Default capacity per person per sprint |
| `warning_threshold_percent` | number | 80 | Percentage at which to warn about overload |
| `users` | object | {} | Per-user overrides: `{"Jordan": {"hours_per_sprint": 35}}` |
| `assignment_guard` | boolean | true | Warn when assigning work to someone over threshold |

**Note:** `capacity.users` allows per-person capacity overrides. If a user isn't listed, `default_hours_per_sprint` applies.

## System Prompt Additions

### Add new section after Proactive Alerting:

```
# ═══════════════════════════════════════════
# CAPACITY PLANNING
# ═══════════════════════════════════════════

Capacity planning translates task effort into workload visibility. It requires
the Effort Logging feature (v1.2) — specifically the Hours Estimated field
and the effort.hours_mapping config.

## Calculating Capacity

For each team member, calculate:

- **Sprint Capacity** = `capacity.users.{name}.hours_per_sprint` or
  `capacity.default_hours_per_sprint` (default: 30)

- **Assigned Load** = Sum of Hours Estimated for tasks where:
  - Assignee = this person
  - Sprint = current sprint
  - Status IN (To Do, In Progress, In Review)

- **Actual Burn** = Sum of Hours Spent for tasks where:
  - Assignee = this person
  - Sprint = current sprint

- **Remaining Capacity** = Sprint Capacity - Assigned Load

- **Utilization %** = (Assigned Load / Sprint Capacity) × 100

- **Capacity Status**:
  - 🟢 Green: utilization < 60%
  - 🟡 Yellow: utilization 60-80%
  - 🔴 Red: utilization > warning_threshold_percent (default 80%)

## Assignment Guard

When `capacity.assignment_guard` is true and the agent is about to assign
or create a task for someone:

1. Calculate their current utilization.
2. Calculate what utilization would be AFTER the new assignment.
3. If post-assignment utilization > warning_threshold_percent:
   - Warn: "{Person} is at {current}% capacity ({hours}h/{cap}h). This
     {effort} task would put them at {new}%. Assign anyway?"
   - Suggest alternatives: "Here's who has bandwidth: {list of people
     under 60% with their current utilization}"
4. If the user confirms, proceed with assignment.
5. Never block an assignment — only warn.

## Capacity Queries

When the user asks about capacity:

### "Show me team capacity" / "Team workload"
Present a per-person table:

| Person | Tasks (Active) | Sprint Load | Capacity | Utilization | Status |
|--------|---------------|-------------|----------|-------------|--------|
| Jordan | 5 in progress, 3 to do | 28h | 30h | 93% | 🔴 |
| Alex   | 2 in progress, 1 to do | 12h | 30h | 40% | 🟢 |
| Sam    | 4 in progress, 2 to do | 22h | 35h | 63% | 🟡 |

### "Who has bandwidth?" / "Who can take this?"
Filter to people under 60% utilization, show remaining hours.

### "Who's overloaded?"
Filter to people over warning_threshold_percent, show how far over.

### "Can we fit [task] in this sprint?"
Calculate total team remaining capacity vs the task's Hours Estimated.
If it fits with a specific person, suggest them. If nobody has room,
suggest options: push to next sprint, split the task, or overload with warning.

### "Sprint planning" / "Plan the sprint"
Present:
1. Current sprint capacity (total team hours)
2. Already assigned hours
3. Remaining capacity
4. Backlog items sorted by priority with their hour estimates
5. Suggest which backlog items fit in remaining capacity

## Velocity Tracking

When asked about velocity or sprint performance:

1. Query completed tasks in previous sprints.
2. Calculate per-sprint:
   - Tasks completed (count)
   - Hours estimated (sum of Hours Estimated on completed tasks)
   - Hours actual (sum of Hours Spent on completed tasks)
   - Completion rate (tasks completed / tasks assigned at sprint start)
3. Show trend over last 3-4 sprints if data exists.
4. Use average velocity to inform capacity recommendations:
   "Based on your last 3 sprints, the team averages {N}h of actual output
   per sprint. You're currently planning {M}h for this sprint."
```

## Agent Patterns

```markdown
### Team Capacity Dashboard

> "Show me team capacity"

Per-person breakdown of sprint load vs capacity with color-coded status. Includes active task count, estimated hours, and utilization percentage.

### Smart Assignment

> "Assign the API refactor task to Jordan"

Agent checks Jordan's current load before assigning. If Jordan is over 80% capacity, the agent warns and suggests who else has bandwidth. Proceeds with assignment if user confirms.

### Sprint Planning Assistant

> "Help me plan Sprint 2"

Agent shows remaining team capacity, then pulls prioritized backlog items with their effort estimates. Suggests which items fit and which need to wait, based on both priority and capacity constraints.

### Velocity Report

> "How did we do last sprint?"

Agent compares planned vs actual: tasks completed, hours estimated vs spent, and per-person performance. Shows trend if multiple sprints of data exist.
```

---

# COMBINED CONFIG TEMPLATE ADDITIONS

Add these three sections to the config template (top-level, alongside `workspace`, `databases`, `migrations`, etc.):

```json
{
  "effort": {
    "hours_mapping": {
      "XS": 1,
      "S": 2,
      "M": 4,
      "L": 8,
      "XL": 16
    },
    "prompt_on_completion": true,
    "track_variance": true,
    "default_log_unit": "hours"
  },
  "alerts": {
    "enabled": true,
    "run_on_briefing": true,
    "auto_ticket_on_critical": true,
    "checks": {
      "stale_tickets": {
        "enabled": true,
        "threshold_days": 7,
        "severity": "warning"
      },
      "stale_critical_tickets": {
        "enabled": true,
        "threshold_hours": 24,
        "severity": "critical",
        "applies_to": ["P0"]
      },
      "stale_high_tickets": {
        "enabled": true,
        "threshold_days": 3,
        "severity": "warning",
        "applies_to": ["P1"]
      },
      "overloaded_assignees": {
        "enabled": true,
        "max_open_tasks": 8,
        "severity": "warning"
      },
      "orphan_tasks": {
        "enabled": true,
        "severity": "info"
      },
      "unassigned_work": {
        "enabled": true,
        "severity": "warning",
        "min_status": "Triaged"
      },
      "empty_engagements": {
        "enabled": true,
        "threshold_days": 30,
        "severity": "warning"
      },
      "upcoming_deadlines": {
        "enabled": true,
        "lookahead_days": 3,
        "severity": "info"
      },
      "sprint_overflow": {
        "enabled": true,
        "severity": "warning"
      },
      "effort_variance": {
        "enabled": true,
        "threshold_percentage": 50,
        "severity": "info"
      }
    }
  },
  "capacity": {
    "enabled": true,
    "sprint_duration_weeks": 2,
    "default_hours_per_sprint": 30,
    "warning_threshold_percent": 80,
    "users": {},
    "assignment_guard": true
  }
}
```

---

# UPDATED TASKS DATABASE SCHEMA

After v1.2 migration, the Tasks database config entry adds:

```json
{
  "databases": {
    "tasks": {
      "optional_fields": ["Hours Spent", "Hours Estimated", "...existing fields..."],
      "number_properties": {
        "Hours Spent": "number",
        "Hours Estimated": "number"
      }
    }
  }
}
```

# UPDATED TICKETS DATABASE SCHEMA

After v1.2 migration, the Tickets database config entry adds:

```json
{
  "databases": {
    "tickets": {
      "enums": {
        "Type": ["Request", "Bug", "Decision", "Alert", "Proposal"]
      }
    }
  }
}
```

---

# UPDATED WORKFLOWS

Add to the `workflows` section of the config:

```json
{
  "workflows": {
    "effort_logging": "Set Effort Estimate → Auto-populate Hours Estimated → Complete task → Log Hours Spent → Variance comment",
    "health_check": "Run enabled alert checks → Group by severity → Present summary → Auto-ticket critical items → Offer actions",
    "capacity_check": "Calculate per-user load → Compare to sprint capacity → Flag overloaded → Suggest rebalancing",
    "morning_briefing_v2": "Run health checks → Alert summary → In progress items → Blocked items → Due dates → Capacity snapshot"
  }
}
```

---

# ONBOARDING CHANGES

During onboarding Step 2 (Personalize), add after the Project Codes section:

```
### Effort & Capacity Defaults
"I'll set up time tracking on your tasks. The default mapping is:
XS = 1 hour, S = 2 hours, M = 4 hours, L = 8 hours, XL = 16 hours.

For capacity planning, the default is 30 hours per person per sprint
(2-week sprints). Want to adjust any of these?"

→ Store customized values in config.
→ If they have no team yet (solo founder), note that capacity planning
  activates when they add team members.
```

---

# IMPLEMENTATION SEQUENCE

### Step 1: Schema Migration (MCP operations)
```
1. notion-update-data-source on Tasks:
   ADD COLUMN "Hours Spent" NUMBER;
   ADD COLUMN "Hours Estimated" NUMBER

2. notion-update-data-source on Tickets:
   ADD COLUMN "Type" SELECT('Request':blue, 'Bug':red, 'Decision':purple, 'Alert':orange, 'Proposal':green)
```

### Step 2: System Prompt Update
- Add Effort Tracking subsection to Operations Mode → Tasks
- Add Proactive Alerting section
- Add Capacity Planning section
- Update Migration Registry with v1.1 → v1.2 entry
- Update version comment to v1.2

### Step 3: Config Template Update
- Add `effort`, `alerts`, `capacity` top-level sections
- Add new fields to tasks and tickets database entries
- Add new workflows
- Update version to 1.2, changelog

### Step 4: Agent Patterns Update
- Add Effort Logging patterns (log time, time report, sprint summary)
- Add Alerting patterns (morning briefing v2, health check, targeted alerts)
- Add Capacity patterns (team capacity, smart assignment, sprint planning, velocity)

### Step 5: Documentation
- Update architecture.md with Tier 1 feature descriptions
- Add troubleshooting entries for common issues
- Update enum-reference.md with new Type enum on Tickets
